module.exports = {
    doParcel : function (element) {
        let id = element.id;
        let date = element.date;
        let items = element.items;
        for (let i = 0; i < items.length; i++) {

        }
    }
};